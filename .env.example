# Environment variables for pRouting backend

# Direct URLs to geospatial data files (replaces Google Cloud Storage)
# SHAPEFILE_URL=https://example.com/path/to/parcels.zip
# ATTR_CSV_URL=https://example.com/path/to/attributes.csv

# Server configuration
# PORT=5000